# Build output
build/
build-*/
*.o
*.a
*.so

# Editor / tooling caches
.cache/
.vscode/
compile_commands.json

# Qt
*.autosave
moc_*.cpp
qrc_*.cpp
ui_*.h

# Misc
*.miff
*.log

# Reference material: the Python corsair-ctl skeleton this project replaced.
reference/

# Design source: the canvas export the Electron UI is built from.
# Large binary, kept locally rather than in history.
*.zip

# Agent build output
agent/build/
